class Movie {
  constructor(title, releaseYear, voteAverage, overview, posterPath) {
    this.title = title;
    this.releaseYear = releaseYear;
    this.voteAverage = voteAverage;
    this.overview = overview;
    this.posterPath = posterPath;
  }
}

module.exports = Movie;
